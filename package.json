{
    "name": "gianlucagiacometti-web-toolbox-build",
    "private": true,
    "description": "Build tooling for gianlucagiacometti/web-toolbox assets.",
    "scripts": {
        "minify": "node scripts/minify-assets.js",
        "build": "npm run minify"
    },
    "devDependencies": {
        "clean-css": "^5",
        "terser": "^5"
    },
    "engines": {
        "node": ">=18"
    }
}
